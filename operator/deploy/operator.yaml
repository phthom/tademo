# Generated by IBM TransformationAdvisor
# Mon Nov 04 18:57:03 GMT 2019
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockquote10war-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: stockquote10war-operator
  template:
    metadata:
      labels:
        name: stockquote10war-operator
    spec:
      serviceAccountName: stockquote10war-operator
      containers:
        - name: stockquote10war-operator
          image: openliberty/operator:0.0.1
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "stockquote10war-operator"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 3
            preference:
              matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
